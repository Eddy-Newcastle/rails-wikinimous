

<% @articles.each do |article| %>
  <h2><%= link_to article.title, article_path(article) %></h2>
  <p><%= article.content %></p>
  <%= link_to "🚮", article_path(article), method: :delete, data: { confirm: "Are you sure?" } %>
<% end %>

<li><%= link_to "Create a new article", new_path %></li>
